import { RouteEntry } from "../../route-entry"

export const metadata = { title: "Confirmation de commande" }

export default function ConfirmationRoute() {
  return <RouteEntry view="confirmation" />
}
